/**
 * When true, release scoring uses algorithm V2 (craft quality + critics + sales).
 * V1 (qualityEngine moving-target) remains available for comparison tests.
 */
export const USE_ALGORITHM_V2 = true;
