/**
 * Curated room art. Components ask getRoomArt() — never raw /attachments paths.
 */
export {
  ROOM_ART_LADDER,
  MENU_ROOM_ART,
  DEPARTMENT_ROOMS,
  LAB_ART,
  SCREEN_ROOM,
  roomArtForOffice,
  eraIdForOffice,
  departmentRoom,
  labRoom,
  labTierFor,
  screenRoomArt,
  type RoomArtDef,
  type RoomEraId,
  type DepartmentRoomId,
  type LabTier,
  type ScreenRoomKey,
} from "../roomArt";
